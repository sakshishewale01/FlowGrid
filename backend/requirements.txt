# ==============================================================================
# FlowGrid - Backend Dependencies
# ==============================================================================

# Core Web Framework
# FastAPI provides high-performance asynchronous API routing, automatic OpenAPI/Swagger docs,
# and native request validation using Pydantic.
fastapi>=0.110.0,<1.0.0

# ASGI Server
# Uvicorn is a lightning-fast ASGI web server implementation used to run FastAPI apps.
uvicorn[standard]>=0.28.0,<1.0.0

# Data Validation and Settings Management
# Pydantic validates request and response schemas; pydantic-settings loads .env configurations.
pydantic[email]>=2.6.0,<3.0.0
pydantic-settings>=2.2.0,<3.0.0
email-validator>=2.0.0

# Database & ORM Layer (Phase 2 & 3)
# SQLAlchemy 2.0 provides modern declarative mapping, typing support, and connection pooling.
sqlalchemy>=2.0.0,<3.0.0

# PostgreSQL Driver
# psycopg 3 (with binary package) is the recommended modern PostgreSQL driver for Python.
psycopg[binary]>=3.1.18

# Database Schema Migrations
# Alembic handles version-controlled, repeatable schema migrations for SQLAlchemy.
alembic>=1.13.0,<2.0.0

# Authentication & Security (Phase 4)
# pwdlib with Argon2 handles secure password hashing and verification.
pwdlib[argon2]>=0.2.0

# PyJWT handles JSON Web Token (JWT) encoding, decoding, and verification.
pyjwt>=2.8.0

# Testing Utilities
pytest>=8.0.0,<9.0.0
httpx>=0.27.0,<1.0.0
